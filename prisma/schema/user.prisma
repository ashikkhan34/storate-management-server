model User {
    id           String @id @default(uuid())
    name         String
    email        String @unique
    password     String
    totalStorage Int    @default(15360)
    usedStorage  Int    @default(0)

    files  File[]
    notes  Note[]
    upload Upload[]
    //   favorites Favorite[]

    createdAt DateTime @default(now())
}
