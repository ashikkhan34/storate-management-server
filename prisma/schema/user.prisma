model User {
    id       String @id @default(uuid())
    name     String
    email    String @unique
    password String
    storage  Int    @default(15360)
    used     Int    @default(0)

    //   files     File[]
    notes Note[]
    //   favorites Favorite[]

    createdAt DateTime @default(now())
}
