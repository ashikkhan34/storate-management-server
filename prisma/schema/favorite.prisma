model Favorite {
    id     String @id @default(cuid())
    userId String

    itemId   String // file/note/image id
    itemType String // "FILE" | "NOTE" | "IMAGE"

    createdAt DateTime @default(now())

    @@unique([userId, itemId, itemType]) // duplicate prevent
}
